# ROADMAP — текущее состояние и планы

## Выполнено

- UI/контент: обновлены публичные страницы и удалены упоминания учебного проекта. Изменены: `templates/base.html`, `templates/about.html`, `templates/index.html`, `templates/contacts.html`, `templates/admin.html`.
- Sticky footer: переработан CSS (см. `static/css/main.css`).
- Unit/integration тесты: добавлены и прогнаны локально (см. `tests/`).
- E2E: добавлен примерный сценарий Playwright в `tests/e2e/` (по умолчанию пропущен).
- Скрипт для пуша: `scripts/push_to_main.py` создан (push не выполнён автоматически).
- Документация: `README.md` и `ROADMAP.md` обновлены.
- Ручной QA: частичная проверка ключевых сценариев (регистрация, резюме, отклик, админ-панель).
- Результаты локального прогона тестов: `pytest` — 20 passed, 3 skipped.

## Текущее состояние

- Бекенд: Flask, Flask-SQLAlchemy, Flask-Login, WTForms, Flask-Admin.
- Фронтенд: Jinja-шаблоны, базовый CSS, исправлен sticky footer.
- Функции: регистрация/логин, резюме, вакансии, отклики, админ-панель.
- Тесты: unit/integration тесты — добавлены и работают (см. `tests/`). E2E — пример в `tests/e2e/` (пропущен по умолчанию).

## Краткосрочные задачи (1–2 недели)

- Закрепить все UI-патчи и проверки accessibility.
- Добавить больше unit-тестов для граничных сценариев и ролей.
- Интегрировать базовый CI (GitHub Actions): запуск `pytest` на push/PR.
- Завершить E2E набор сценариев (регистрация, создание резюме, отклик, employer flows, admin flows).

## Среднесрочные задачи (1–2 месяца)

- Развернуть сборку и деплой на staging (Docker/Gunicorn + nginx).
- Добавить авторизацию через OAuth (опционально).
- Улучшить UX: карточки вакансий, фильтры, пагинация.
- Security review: CSP, rate limiting, защиту форм и паролей.

## Долгосрочные задачи

- Мониторинг и метрики: Sentry, uptime checks.
- CI: автодеплой в staging и проверка E2E перед merge.
- Multi-tenant / коммерческая логика (оплата, подписки).

## Как помочь

- Запустить локально, прогнать тесты и открывать issues для багов.
- Добавлять тесты в `tests/` и E2E сценарии в `tests/e2e/`.
